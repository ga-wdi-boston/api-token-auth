'use strict';

//
// define games objects
//

// create new game
const game = function(id, cells, over, player_x, player_o){
  id = id;
  cells = cells;
  over = over;
  player_x = player_x;
  player_o = player_o;
};

module.exports = {
  game,
};
